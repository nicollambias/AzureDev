<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <% books.forEach((book) => { %>
      <div>
        <h3><%=book.title%></h3>
        <h4><%=book.author%></h4>
      </div>
    <% }); %>
  </body>
</html>
